<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <a class="navbar-brand" href="/">Animal list</a>
    <div style="width: 30px"></div>
    <ul class="navbar-nav mr-auto">
      
      <li class="nav-item">
        <% if(sel1 == 'true') { %>
          <strong><a class="nav-link text-white" href="/">Animals</a></strong>
        <% }; %>
        <% if(sel1 != 'true'){ %>
          <a class="nav-link" href="/">Animals</a>
        <% }; %>
      </li>

            <li class="nav-item">
        <% if(sel2 == 'true') { %>
          <strong><a class="nav-link text-white" href="/">Add New</a></strong>
        <% }; %>
        <% if(sel2 != 'true'){ %>
          <a class="nav-link" href="/form">Add New</a>
        <% }; %>
      </li>

      <li class="nav-item">
        <% if(sel3 == 'true') { %>
          <strong><a class="nav-link text-white" href="/about">About</a></strong>
        <% }; %>
        <% if(sel3 != 'true'){ %>
          <a class="nav-link" href="/about">About</a>
        <% }; %>
      </li>

    </ul>
</nav>